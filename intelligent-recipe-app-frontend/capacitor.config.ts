import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vinaythakoor.recipeapp',
  appName: 'intelligent-recipe-app-frontend',
  webDir: 'www/browser'
};

export default config;
